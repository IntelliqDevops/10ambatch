---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: proxy
    author: intelliqit
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests:
          memory: 64M
          cpu: 200m
        limits:
          memory: 128M
          cpu: 500m
